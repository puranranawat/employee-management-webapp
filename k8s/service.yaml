apiVersion: v1
kind: Service
metadata:
  name: employee-management-service
spec:
  selector:
    app: employee-management
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
